<% if user_signed_in? %>
<div class="serch_form text_center">
  <%= form_with url: tagsearches_search_path, local: true, method: :get do |f| %>
  <%= f.text_field :word %>
  <%= f.submit "タグ検索" %>
  <% end %>
</div>
<% end %>
